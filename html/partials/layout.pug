include ../mixins/window-title

- var title = "Title"

doctype html
html
  head
    meta(name="viewport" content="width=device-width, initial-scale=1")
    block title
      +window-title(title)
    link(rel="stylesheet" href="index.css")
  body
    header
      block header
        include header
    main
      div.page-title
        block page-title
          h1= title
      div.page-content
        block page-content
          p hello world
